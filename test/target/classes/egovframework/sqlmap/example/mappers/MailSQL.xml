<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="egovframework.example.sample.service.impl.MailMapper">
  
    
    <insert id="insMail" parameterType="mail">
    	insert into mail(mail_id, receiver, title, content, send_date) 
    		values ('M00'||mail_seq.nextval, #{receiver}, #{title}, #{content,jdbcType=CLOB}, sysdate)
    </insert>
  	<insert id="insFileWithMail" parameterType="file">
    	insert into FileStorage (file_id, fname, path, board_id)
    	 values('F00'||file_seq.nextval, #{fname}, #{path}, 'M00'||mail_seq.currval)
    </insert>
  
</mapper>